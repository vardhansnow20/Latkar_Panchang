/**
 * A single quiet pause between History and Explore — not a section
 * with an eyebrow and heading like the others, deliberately, so it
 * reads as a breath rather than another chapter.
 */
export const interlude = {
  statement:
    "Before a wedding is set. Before a journey begins. Before a name is chosen — someone opens this page first.",
  /** Devanagari for "Panchang" — the first real use of the
   * --font-devanagari token reserved since the design-system pass. */
  mark: "पंचांग",
}
